export interface TD {
  text: string;
  ok: boolean;
}
export interface UserInfo {
  name: string;
  nick: string;
  id: string;
  regist_time: string;
  password: string;
  type: string;
  phone: string;
}
